# ═══════════════════════════════════════════
# Python 依赖 - 兼容范围约束
# ═══════════════════════════════════════════

# 向量库
lancedb>=0.10.0,<1.0.0

# 嵌入模型 (BGE-M3 等)
sentence-transformers>=3.0.0,<4.0.0

# 向量运算
numpy>=1.24.0,<3.0.0

# 数据处理 (可选, 被部分脚本引用)
pandas>=2.0.0,<3.0.0

# MySQL 驱动
PyMySQL>=1.1.0,<2.0.0

# BM25 关键词检索 (LanceDB FTS 依赖)
rank-bm25>=0.2.2,<1.0.0
